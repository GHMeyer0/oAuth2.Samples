import React from "react";
import { Typography } from "@material-ui/core";

const Unprotected = () => {
  return (
    <div>
      <Typography>Não protegido</Typography>
    </div>
  );
};

export default Unprotected;
