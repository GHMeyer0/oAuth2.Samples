import { Typography } from "@material-ui/core";
import React from "react";

const Protected = () => {
  return (
    <div>
      <Typography>Protegido</Typography>
    </div>
  );
};

export default Protected;
